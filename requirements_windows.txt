# =====================================================================
# Rubik's Cube Solver - WINDOWS
# =====================================================================

# === Vision & Traitement d'image ===
# OpenCV : Sur Windows, on installe via pip (pas apt)
opencv-python>=4.8.0
numpy>=1.24.0
matplotlib>=3.7.0

# === Traitement d'images (PIL/Pillow) ===
Pillow>=10.0.0

# === Résolution cube ===
kociemba>=1.2.0
RubikTwoPhase>=1.0.0

# === Détection automatique (YOLOv8) ===
ultralytics>=8.0.0

# === Affichage terminal ===
colorama>=0.4.6

# === Interface graphique (Tkinter) ===
# Tkinter : INCLUS avec Python sur Windows
# Rien à installer, mais vérifier lors de l'installation Python
# que l'option "tcl/tk and IDLE" est cochée

# === Tests (optionnel) ===
pytest>=7.4.0

# === Contrôle robot (si Raspberry Pi - ignorer sur Windows) ===
# adafruit-circuitpython-neopixel>=6.3.0
# adafruit-blinka>=8.0.0
# rpi_ws281x>=5.0.0
